export function skillAttack(skillNumber: number, attacker: BattleSceneStatus, defender: BattleSceneStatus) {
	/*
		攻撃に対するバフ→攻撃に対するデバフ→防御に対するバフ→防御に対するデバフ の順で処理をして
		攻撃力・防御力からダメージを算出する
		↑色々変わったから、バフとデバフはひとまとめに処理
		スキルの順番によって効果の度合いが変わるかも
	*/

	const AttackerStrengtheningValue = 0;
}
